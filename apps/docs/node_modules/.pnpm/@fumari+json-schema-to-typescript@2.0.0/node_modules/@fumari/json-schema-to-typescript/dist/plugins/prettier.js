import {
  __spreadValues
} from "../chunk-6FNC3XMI.js";

// src/plugins/prettier.ts
import { format } from "prettier/standalone";
import estreePlugin from "prettier/plugins/estree";
import typescriptPlugin from "prettier/plugins/typescript";
function prettierPlugin(prettierOptions = {
  bracketSpacing: false,
  printWidth: 120,
  semi: true,
  singleQuote: false,
  tabWidth: 2,
  trailingComma: "none",
  useTabs: false
}) {
  return {
    output(code) {
      return format(code, __spreadValues({
        parser: "typescript",
        plugins: [estreePlugin, typescriptPlugin]
      }, prettierOptions));
    }
  };
}
export {
  prettierPlugin
};
