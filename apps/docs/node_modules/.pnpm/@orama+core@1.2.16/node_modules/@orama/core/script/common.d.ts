import { type SSEEvent } from './lib/event-stream.js';
export type ApiKeyPosition = 'header' | 'query-params';
export type ClientRequestInit = Omit<RequestInit, 'method' | 'headers' | 'body'>;
type ReadWriteAuth = {
    readerURL?: string;
    writerURL?: string;
};
type ApiKeyAuth = {
    type: 'apiKey';
    apiKey: string;
};
type JwtAuth = {
    type: 'jwt';
    authJwtURL: string;
    collectionID: string;
    privateApiKey: string;
};
type AuthConfig = ReadWriteAuth & (ApiKeyAuth | JwtAuth);
export declare class Auth {
    private config;
    constructor(config: AuthConfig);
    getRef(target: ClientRequest['target'], init?: ClientRequestInit): Promise<{
        bearer: string;
        baseURL: string;
    }>;
}
export type ClientRequest = {
    target: 'reader' | 'writer';
    method: 'GET' | 'POST' | 'PUT';
    path: string;
    body?: object;
    params?: Record<string, string>;
    init?: ClientRequestInit;
    apiKeyPosition: ApiKeyPosition;
};
export interface ClientConfig {
    auth: Auth;
}
export declare class Client {
    private config;
    constructor(config: ClientConfig);
    request<Output>(req: ClientRequest): Promise<Output>;
    requestStream(req: ClientRequest): Promise<ReadableStream<SSEEvent>>;
    eventSource(req: ClientRequest): Promise<EventSource>;
    getResponse({ method, path, body, params, apiKeyPosition, init, target, }: ClientRequest): Promise<Response>;
}
export declare function safeJSONParse<T = unknown>(data: string, silent?: boolean): T;
export type TrainingSet = typeof TRAINING_SETS[number];
export declare const TRAINING_SETS: readonly ["query_optimizer", "query_planner", "query_filtering"];
export {};
//# sourceMappingURL=common.d.ts.map