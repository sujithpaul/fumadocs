import { t as _objectSpread2 } from "./objectSpread2-C-JiF_cP.js";
import { fromMarkdown } from "mdast-util-from-markdown";
import { gfmFromMarkdown } from "mdast-util-gfm";
import { defaultHandlers, toHast } from "mdast-util-to-hast";
import { ShikiError } from "shiki";
import { createTransformerFactory, rendererRich } from "@shikijs/twoslash";
import { createTwoslasher } from "twoslash";

//#region src/index.ts
let cachedInstance;
function transformerTwoslash(options = {}) {
	var _options$rendererRich;
	const ignoreClass = "nd-copy-ignore";
	function lazyInstance() {
		function get() {
			var _cachedInstance;
			return (_cachedInstance = cachedInstance) !== null && _cachedInstance !== void 0 ? _cachedInstance : cachedInstance = createTwoslasher(options.twoslashOptions);
		}
		const wrapper = (...args) => get()(...args);
		wrapper.getCacheMap = () => get().getCacheMap();
		return wrapper;
	}
	const renderer = rendererRich(_objectSpread2(_objectSpread2({
		classExtra: ignoreClass,
		queryRendering: "line",
		renderMarkdown,
		renderMarkdownInline
	}, options === null || options === void 0 ? void 0 : options.rendererRich), {}, { hast: _objectSpread2({
		hoverToken: { tagName: "Popup" },
		hoverPopup: {
			tagName: "PopupContent",
			properties: { class: ignoreClass }
		},
		hoverCompose: ({ popup, token }) => [popup, {
			type: "element",
			tagName: "PopupTrigger",
			properties: {},
			children: [token]
		}],
		popupDocs: { class: "prose twoslash-popup-docs" },
		popupTypes: {
			tagName: "div",
			class: "twoslash shiki fd-codeblock prose-no-margin",
			children: (v) => {
				if (v.length === 1 && v[0].type === "element" && v[0].tagName === "pre") return v;
				return [{
					type: "element",
					tagName: "code",
					properties: { class: "twoslash-popup-code" },
					children: v
				}];
			}
		},
		popupDocsTags: { class: "prose twoslash-popup-docs twoslash-popup-docs-tags" },
		nodesHighlight: { class: "highlighted-word twoslash-highlighted" }
	}, options === null || options === void 0 || (_options$rendererRich = options.rendererRich) === null || _options$rendererRich === void 0 ? void 0 : _options$rendererRich.hast) }));
	const fn = renderer.lineQuery;
	renderer.lineQuery = function(...args) {
		const result = fn.call(this, ...args);
		const child = result[0].children[0];
		result[0].children[0] = {
			type: "element",
			tagName: "span",
			children: [child]
		};
		return result;
	};
	return createTransformerFactory(lazyInstance(), renderer)(_objectSpread2({ explicitTrigger: true }, options));
}
function renderMarkdown(md) {
	return toHast(fromMarkdown(md.replace(new RegExp("{@link (?<link>[^}]*)}", "g"), "$1"), { mdastExtensions: [gfmFromMarkdown()] }), { handlers: { code: (state, node) => {
		if (!node.lang) return defaultHandlers.code(state, node);
		try {
			var _node$meta;
			return this.codeToHast(node.value, _objectSpread2(_objectSpread2({}, this.options), {}, {
				transformers: [],
				meta: { __raw: (_node$meta = node.meta) !== null && _node$meta !== void 0 ? _node$meta : void 0 },
				lang: node.lang
			})).children[0];
		} catch (e) {
			if (e instanceof Error) console.error(`[fumadocs-twoslash] encountered an error when highlighting codeblock in a Twoslash popup: ${e.message}`);
			if (e instanceof ShikiError) console.error(`[fumadocs-twoslash] if language "${node.lang}" is not found, you may have enabled lazy loading which is not compatible with Twoslash, please consider to define "${node.lang}" in the "langs" option first.`);
			return defaultHandlers.code(state, node);
		}
	} } }).children;
}
function renderMarkdownInline(md, context) {
	const text = context === "tag:param" ? md.replace(new RegExp("^(?<link>[\\w$-]+)", ""), "`$1` ") : md;
	const children = renderMarkdown.call(this, text);
	if (children.length === 1 && children[0].type === "element" && children[0].tagName === "p") return children[0].children;
	return children;
}

//#endregion
export { transformerTwoslash };
//# sourceMappingURL=index.js.map